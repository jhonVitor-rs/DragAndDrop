
export enum Color {
  BLUE,
  GREEN,
  RED,
  YELLOW
}

export enum ColumnType {
  TO_DO = 'To do',
  IN_PROGRESS = 'In Progress',
  BLOCKED = 'Blocked',
  COMPLETED = 'Completed'
}

export enum ItemType {
  TASK = 'Task'
}
